{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": [
      "node"
    ]
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts"  // <--- این خط را اضافه کن
  ],
  "exclude": [
    "src/**/*.spec.ts"
  ]
}